#set( $symbol_pound = "#" )
${symbol_pound} UC-000：用例任务示例

${symbol_pound}${symbol_pound} 一、任务类型

- 类型：用例开发 / 用例迭代

说明：

```text
用例开发：新增一个完整业务能力。
用例迭代：调整或增强已有业务能力。
```

${symbol_pound}${symbol_pound} 二、业务目标

这里描述这个用例要完成的业务目标。

示例：

- 用例开发：调用方提交业务请求后，系统完成一次核心业务动作，并返回处理结果。
- 用例迭代：在已有业务流程中增加新的业务规则、入参、状态或外部协作。

${symbol_pound}${symbol_pound} 三、原有行为

如果是用例迭代，必须描述原有行为。

示例：

- 调用方提交基础请求参数。
- 系统执行已有业务校验。
- 系统保存核心数据。
- 系统返回处理结果。

如果是用例开发，可以填写：

```text
无历史行为，本任务为新增能力。
```

${symbol_pound}${symbol_pound} 四、本次新增或调整行为

描述本次用例要新增或调整什么。

示例：

- 新增一个请求字段。
- 新增一次外部服务校验。
- 新增一个领域规则。
- 新增一个返回字段。
- 调整异常处理逻辑。

${symbol_pound}${symbol_pound} 五、不变行为

如果是用例迭代，必须说明哪些行为不能变。

示例：

- 原有请求方式不变。
- 原有成功主流程不变。
- 未传新增字段时，旧流程不受影响。
- 原有状态流转不变。
- 不改变支付、库存、履约等非本任务边界内的流程。

如果是用例开发，可以填写：

```text
无历史行为约束。
```

${symbol_pound}${symbol_pound} 六、业务边界

本用例负责：

- 接收业务请求
- 校验基础参数
- 执行业务规则
- 持久化核心数据
- 返回处理结果

本用例不负责：

- 不属于当前服务边界的外部业务
- 未在本任务中明确要求的异步流程
- 未在本任务中明确要求的定时任务

${symbol_pound}${symbol_pound} 七、参与角色

- 调用方
- 当前服务
- 外部服务 A，可选
- 外部服务 B，可选

${symbol_pound}${symbol_pound} 八、前置条件

- 前置条件 1
- 前置条件 2
- 前置条件 3

${symbol_pound}${symbol_pound} 九、主流程

1. 调用方通过 HTTP / gRPC / MQ 触发当前用例。
2. adapter 层接收请求，并转换为 Command / Query。
3. app 层执行应用用例。
4. app 层完成流程编排和事务控制。
5. domain 层表达核心业务规则。
6. infrastructure 层完成数据库访问或外部系统调用。
7. app 层返回业务结果。
8. adapter 层转换响应并返回。

${symbol_pound}${symbol_pound} 十、异常流程

${symbol_pound}${symbol_pound}${symbol_pound} 1. 参数不合法

- 触发条件：
- 系统行为：
- 返回结果：

${symbol_pound}${symbol_pound}${symbol_pound} 2. 业务状态不允许

- 触发条件：
- 系统行为：
- 返回结果：

${symbol_pound}${symbol_pound}${symbol_pound} 3. 外部依赖失败

- 触发条件：
- 系统行为：
- 返回结果：

${symbol_pound}${symbol_pound} 十一、业务规则

- 规则 1
- 规则 2
- 规则 3

${symbol_pound}${symbol_pound} 十二、涉及模块

- ${artifactId}-client
- ${artifactId}-domain
- ${artifactId}-infrastructure
- ${artifactId}-app
- ${artifactId}-adapter

${symbol_pound}${symbol_pound} 十三、允许修改范围

根据用例需要修改，下面只是示例：

- ${artifactId}-client/**
- ${artifactId}-domain/**
- ${artifactId}-infrastructure/**
- ${artifactId}-app/**
- ${artifactId}-adapter/**

${symbol_pound}${symbol_pound} 十四、禁止修改范围

根据用例需要修改，下面只是示例：

- ${artifactId}-start/**
- pom.xml
- AGENTS.md
- .harness/checks/**
- scripts/**

${symbol_pound}${symbol_pound} 十五、实现要求

${symbol_pound}${symbol_pound}${symbol_pound} client 层

- 定义 Command / Query / DTO / Result。
- 如果需要 gRPC，维护 proto 契约。

${symbol_pound}${symbol_pound}${symbol_pound} domain 层

- 定义领域对象、领域枚举和领域规则。
- 不引入技术依赖。

${symbol_pound}${symbol_pound}${symbol_pound} infrastructure 层

- 定义 DO / Mapper / Repository / Gateway / Converter。
- 不编排完整业务流程。

${symbol_pound}${symbol_pound}${symbol_pound} app 层

- 编排完整用例流程。
- 控制事务边界。
- 调用 domain 和 infrastructure。

${symbol_pound}${symbol_pound}${symbol_pound} adapter 层

- 提供 HTTP / gRPC / MQ 接入。
- 只做协议转换。
- 不直接访问 Mapper。

${symbol_pound}${symbol_pound} 十六、验收标准

- 全工程编译通过。
- 架构边界检查通过。
- 修改范围检查通过。
- 主流程有明确实现。
- 异常流程有明确处理。
- 没有修改禁止范围内的文件。
- 如果是用例迭代，原有不变行为没有被破坏。
- adapter 没有业务编排。
- domain 没有基础设施依赖。

${symbol_pound}${symbol_pound} 十七、验证命令

```bash
./.harness/checks/verify-all.sh
./.harness/checks/scope-check.sh .harness/任务/用例/UC-000-用例任务示例.md
```

${symbol_pound}${symbol_pound} 十八、注意事项

- 真实任务请复制本文件后重命名。
- 不要直接在示例文件上开发。
- 用例迭代必须写清楚原有行为和不变行为。
- 任务描述越清晰，Agent 越不容易自由发挥。