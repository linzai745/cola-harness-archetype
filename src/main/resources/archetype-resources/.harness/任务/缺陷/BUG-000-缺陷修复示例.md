#set( $symbol_pound = "#" )
${symbol_pound} BUG-000：缺陷修复示例

${symbol_pound}${symbol_pound} 一、任务类型

- 类型：缺陷修复

${symbol_pound}${symbol_pound} 二、问题描述

描述当前缺陷现象。

示例：

当请求参数为空时，接口没有返回明确的业务错误，而是抛出系统异常。

${symbol_pound}${symbol_pound} 三、影响范围

- 影响接口：
- 影响模块：
- 影响业务流程：

${symbol_pound}${symbol_pound} 四、复现步骤

1. 准备请求参数。
2. 调用接口。
3. 观察错误现象。

${symbol_pound}${symbol_pound} 五、期望行为

描述修复后应该是什么行为。

示例：

当参数为空时，系统返回明确的参数错误，不抛出系统异常。

${symbol_pound}${symbol_pound} 六、原因分析

如果已经知道原因，在这里填写。

如果暂时不知道，可以写：

```text
原因待 Agent 结合代码定位，但修复范围必须限制在本任务允许范围内。
```

${symbol_pound}${symbol_pound} 七、允许修改范围

根据实际缺陷填写。

示例：

- ${artifactId}-adapter/**
- ${artifactId}-app/**

${symbol_pound}${symbol_pound} 八、禁止修改范围

根据实际缺陷填写。

示例：

- ${artifactId}-domain/**
- ${artifactId}-infrastructure/**
- ${artifactId}-start/**
- pom.xml
- AGENTS.md
- .harness/checks/**
- scripts/**

${symbol_pound}${symbol_pound} 九、修复要求

- 只修复当前缺陷。
- 不顺手重构无关代码。
- 不扩大业务边界。
- 不修改无关模块。
- 如果发现问题根因不在允许范围内，停止并说明原因。

${symbol_pound}${symbol_pound} 十、验收标准

- 缺陷复现路径已修复。
- 原有主流程不受影响。
- 编译通过。
- 架构边界检查通过。
- 修改范围检查通过。

${symbol_pound}${symbol_pound} 十一、验证命令

```bash
./.harness/checks/verify-all.sh
./.harness/checks/scope-check.sh .harness/任务/缺陷/BUG-000-缺陷修复示例.md
```

${symbol_pound}${symbol_pound} 十二、注意事项

- 缺陷修复任务必须尽量小。
- 不允许借修复缺陷做大范围重构。
- 如果需要重构，请单独创建重构任务。