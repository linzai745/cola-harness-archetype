#set( $symbol_pound = "#" )
${symbol_pound} RF-000：重构任务示例

${symbol_pound}${symbol_pound} 一、任务类型

- 类型：重构优化

${symbol_pound}${symbol_pound} 二、重构目标

描述本次重构要解决什么结构性问题。

示例：

当前应用服务中存在过多参数转换逻辑，需要抽取到 assembler，降低 app 层代码复杂度。

${symbol_pound}${symbol_pound} 三、当前问题

- 问题 1
- 问题 2
- 问题 3

${symbol_pound}${symbol_pound} 四、重构边界

本次重构负责：

- xxx
- xxx

本次重构不负责：

- xxx
- xxx

${symbol_pound}${symbol_pound} 五、允许修改范围

根据实际重构范围填写。

示例：

- ${artifactId}-app/**
- ${artifactId}-adapter/**

${symbol_pound}${symbol_pound} 六、禁止修改范围

根据实际情况填写。

示例：

- ${artifactId}-domain/**
- ${artifactId}-infrastructure/**
- ${artifactId}-start/**
- pom.xml
- AGENTS.md
- .harness/checks/**
- scripts/**

${symbol_pound}${symbol_pound} 七、重构要求

- 不改变对外接口语义。
- 不改变核心业务行为。
- 不扩大业务边界。
- 不引入任务未要求的新框架。
- 不修改无关模块。
- 如果需要修改禁止范围，停止并说明原因。

${symbol_pound}${symbol_pound} 八、验收标准

- 全工程编译通过。
- 架构边界检查通过。
- 修改范围检查通过。
- 原有主流程不受影响。
- 代码结构比重构前更清晰。
- 没有引入新的循环依赖。

${symbol_pound}${symbol_pound} 九、验证命令

```bash
./.harness/checks/verify-all.sh
./.harness/checks/scope-check.sh .harness/任务/重构/RF-000-重构任务示例.md
```

${symbol_pound}${symbol_pound} 十、注意事项

- 重构任务必须明确“不改变行为”还是“允许行为调整”。
- 如果涉及行为变化，应该拆成用例任务或缺陷修复任务。
- 不要把多个不相关重构塞进一个任务。