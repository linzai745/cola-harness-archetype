#set( $symbol_pound = "#" )
${symbol_pound} 项目上下文

${symbol_pound}${symbol_pound} 一、项目名称

${rootArtifactId}

${symbol_pound}${symbol_pound} 二、项目定位

本项目是基于 COLA 多模块分层架构生成的 Java 后端服务。

项目创建后，请在这里补充当前服务的真实业务边界、外部依赖、通信方式和技术选型。

${symbol_pound}${symbol_pound} 三、默认技术栈

- Java ${javaVersion}
- Spring Boot ${springBootVersion}
- Maven 多模块
- COLA 分层思想
- Lombok
- MapStruct
- MyBatis-Plus
- 可选 gRPC
- 可选 Redis
- 可选 Sa-Token
- 可选 MQ

${symbol_pound}${symbol_pound} 四、默认模块

```text
${rootArtifactId}-client
${rootArtifactId}-adapter
${rootArtifactId}-app
${rootArtifactId}-domain
${rootArtifactId}-infrastructure
${rootArtifactId}-start
```
${symbol_pound}${symbol_pound} 五、模块说明

* ${rootArtifactId}-client：对外契约。
* ${rootArtifactId}-adapter：接入适配。
* ${rootArtifactId}-app：应用编排。
* ${rootArtifactId}-domain：领域模型。
* ${rootArtifactId}-infrastructure：基础设施实现。
* ${rootArtifactId}-start：启动装配。

${symbol_pound}${symbol_pound} 六、业务边界

请补充：
```text
本服务负责：
- 

本服务不负责：
- 
```

${symbol_pound}${symbol_pound} 七、接口方式
请补充：
```text
HTTP 接口：
- 

gRPC 接口：
- 

MQ 消费：
- 

定时任务：
- 
```

${symbol_pound}${symbol_pound} 八、外部依赖
请补充：
```text
依赖服务：
- 

依赖中间件：
- 

依赖第三方：
- 
```

${symbol_pound}${symbol_pound} 九、当前阶段目标

请补充当前阶段目标，例如：
```text
先完成基础工程骨架；
再完成核心领域模型；
再完成基础 CRUD；
再接入认证、缓存、MQ、监控等能力。
```
